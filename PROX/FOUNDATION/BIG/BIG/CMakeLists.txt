INCLUDE_DIRECTORIES(
  ${PROJECT_SOURCE_DIR}/PROX/FOUNDATION/BIG/BIG/include 
  ${Boost_INCLUDE_DIR}
  ${Boost_BINDINGS_INCLUDE_DIR} 
)

FILE(GLOB
  BIG_SOURCES
  ${PROJECT_SOURCE_DIR}/PROX/FOUNDATION/BIG/BIG/src/*.cpp 
  )

FILE(GLOB 
  BIG_HEADERS 
  ${PROJECT_SOURCE_DIR}/PROX/FOUNDATION/BIG/BIG/include/*.h 
  )

ADD_LIBRARY( 
  big 
  ${BIG_SOURCES} 
  ${BIG_HEADERS} 
  )

TARGET_LINK_LIBRARIES(
  big
  ${Boost_LIBRARIES} 
  ${BIG_BLAS_LIBRARY} 
  )

SET_TARGET_PROPERTIES(big PROPERTIES DEBUG_POSTFIX "D" )
SET_TARGET_PROPERTIES(big PROPERTIES ARCHIVE_OUTPUT_DIRECTORY                "${PROJECT_SOURCE_DIR}/lib/")
SET_TARGET_PROPERTIES(big PROPERTIES ARCHIVE_OUTPUT_DIRECTORY_DEBUG          "${PROJECT_SOURCE_DIR}/lib/")
SET_TARGET_PROPERTIES(big PROPERTIES ARCHIVE_OUTPUT_DIRECTORY_RELEASE        "${PROJECT_SOURCE_DIR}/lib/")
SET_TARGET_PROPERTIES(big PROPERTIES ARCHIVE_OUTPUT_DIRECTORY_MINSIZEREL     "${PROJECT_SOURCE_DIR}/lib/")
SET_TARGET_PROPERTIES(big PROPERTIES ARCHIVE_OUTPUT_DIRECTORY_RELWITHDEBINFO "${PROJECT_SOURCE_DIR}/lib/")
