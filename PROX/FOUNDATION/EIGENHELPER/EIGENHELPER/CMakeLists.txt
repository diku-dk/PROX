INCLUDE_DIRECTORIES(
  ${PROJECT_SOURCE_DIR}/PROX/FOUNDATION/EIGENHELPER/EIGENHELPER/include
)

FILE(
  GLOB
  EIGENHELPER_SOURCES
  ${PROJECT_SOURCE_DIR}/PROX/FOUNDATION/EIGENHELPER/EIGENHELPER/src/*.cpp
  )

FILE(
  GLOB
  EIGENHELPER_HEADERS
  ${PROJECT_SOURCE_DIR}/PROX/FOUNDATION/EIGENHELPER/EIGENHELPER/include/*.h
  )

ADD_LIBRARY(
  eigenhelper
  ${EIGENHELPER_SOURCES}
  ${EIGENHELPER_HEADERS}
  )

#TARGET_LINK_LIBRARIES( eigenhelper )

SET_TARGET_PROPERTIES(eigenhelper PROPERTIES DEBUG_POSTFIX "D" )

SET_TARGET_PROPERTIES(eigenhelper PROPERTIES ARCHIVE_OUTPUT_DIRECTORY                "${PROJECT_SOURCE_DIR}/lib/")
SET_TARGET_PROPERTIES(eigenhelper PROPERTIES ARCHIVE_OUTPUT_DIRECTORY_DEBUG          "${PROJECT_SOURCE_DIR}/lib/")
SET_TARGET_PROPERTIES(eigenhelper PROPERTIES ARCHIVE_OUTPUT_DIRECTORY_RELEASE        "${PROJECT_SOURCE_DIR}/lib/")
SET_TARGET_PROPERTIES(eigenhelper PROPERTIES ARCHIVE_OUTPUT_DIRECTORY_MINSIZEREL     "${PROJECT_SOURCE_DIR}/lib/")
SET_TARGET_PROPERTIES(eigenhelper PROPERTIES ARCHIVE_OUTPUT_DIRECTORY_RELWITHDEBINFO "${PROJECT_SOURCE_DIR}/lib/")
