IF(NOT IS_DIRECTORY "${PROJECT_SOURCE_DIR}/3RDPARTY/BOOST_BINDINGS/boost_bindings/")
  MESSAGE("Run the install_boost_bindings script from the BOOST_BINDING folder. This will checkout boost bindings from an svn server and put them into proper place")
ENDIF()